downloading the pcap file shows us a network traffic capture.

upon running a tcp stream we see that a jpeg image is getting passed through.

exporting objects gives us the image

the flag is printed inside

actf{0ver_th3_w1re}
