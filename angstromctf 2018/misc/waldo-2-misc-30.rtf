downloading the zip and unzipping gives 500 waldo images.

sorting it by size shows 1 file to be 28 bytes in size. 
opening it doesn\'92t allow it to open since its not an image.

running file command on it tells that the file is ascii text.
running cat spits the flag

actf{r3d_4nd_wh1t3_str1p3s}
